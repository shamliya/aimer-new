import React from 'react';
import './Loader.css'; // Import any CSS needed for styling

const Loader = () => {
  return (
    <div className="loader">
      {/* Example loader styling */}
      <div className="spinner"></div>
    </div>
  );
};

export default Loader;
